/*
 *  UCF COP3330 Fall 2021 Assignment 3 Solution
 *  Copyright 2021 Kimari Guthre
 */
package baseline;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class ReturnValidatorTest {

    /*
    This test was commented out because it now refers to a private method. Un-private it if you want to test!
    @Test
    void testValidateInput() {
        assertTrue(ReturnValidator.validateInput("123"), "Does not properly identify ints");
        assertFalse(ReturnValidator.validateInput("0"), "Does not properly identify zeroes (int)");
        assertTrue(ReturnValidator.validateInput("123.456"), "Does not properly identify floats");
        assertFalse(ReturnValidator.validateInput("0.0"),
                "Does not properly identify zeroes (decimals)");
        assertFalse(ReturnValidator.validateInput(""), "Does not properly identify no input");
    }
     */
}