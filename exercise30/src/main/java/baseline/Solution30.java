package baseline;

public class Solution30 {
    public static void main(String[] args) {
        //First, find the length of the bottom-rightmost number of the multiplication table and add two to that.
        //(The above is for proper formatting, we'll refer to this as MULTIPLICATION_TABLE_LENGTH_OF_LARGEST_INT)
        //In the higher loop, execute the lower loop once, then add a line break.
        //In the lower loop, print the result of getMultiplicationTableEntry(higherLoopIteration, lowerLoopIteration).
        //Finally, print that string.
    }
}
