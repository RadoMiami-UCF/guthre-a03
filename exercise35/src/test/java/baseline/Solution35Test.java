/*
 *  UCF COP3330 Fall 2021 Assignment 3 Solution
 *  Copyright 2021 Kimari Guthre
 */
package baseline;

import org.junit.jupiter.api.Test;

import java.util.ArrayList;
import java.util.Arrays;

import static org.junit.jupiter.api.Assertions.*;

class Solution35Test {

    /*
    These tests were commented out because they refer to now private methods. Make them public to use the tests!
    @Test
    void testHasNewName() {
        assertFalse(Solution35.hasNewName(Arrays.asList("Sample name 1", "Sample name 2"), ""),
                "Does not properly deal with empty names");
        ArrayList<String> testArrayList = new ArrayList<>(Arrays.asList("Sample name 1", "Sample name 2"));
        assertTrue(Solution35.hasNewName(testArrayList, "Sample name 3"),
                "Does not properly identify non-empty names");
        assertEquals(Arrays.asList("Sample name 1", "Sample name 2", "Sample name 3"), testArrayList,
                "Does not properly update arrayList in event of non-empty name");
    }

    @Test
    void testPickName() {
        assertEquals("The winner is... Johnathan.",
                Solution35.pickName(Arrays.asList("Chris", "Johnathan", "Sarah", "Samantha"), 1),
                "Cannot properly identify winner given index");
    }
     */
}